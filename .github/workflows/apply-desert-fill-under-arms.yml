name: Apply desert under-arms compaction

on:
  push:
    branches:
      - redesign/desert-fill-under-arms
    paths:
      - tools/apply-desert-fill-under-arms.cjs
      - .github/workflows/apply-desert-fill-under-arms.yml
  pull_request:
    branches:
      - main
    paths:
      - tools/apply-desert-fill-under-arms.cjs
      - .github/workflows/apply-desert-fill-under-arms.yml

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: redesign/desert-fill-under-arms
          fetch-depth: 0

      - name: Validate migration syntax
        run: node --check tools/apply-desert-fill-under-arms.cjs

      - name: Apply geometry changes
        run: node tools/apply-desert-fill-under-arms.cjs

      - name: Validate desert scene
        run: |
          node tools/check-desert.cjs
          for file in js/data/desert/*.js js/runtime/interactions-desert.js js/runtime/tables-desert.js; do
            node --check "$file"
          done

      - name: Remove one-time migration files
        run: |
          rm tools/apply-desert-fill-under-arms.cjs
          rm .github/workflows/apply-desert-fill-under-arms.yml

      - name: Commit generated source
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -- \
            desert.html \
            docs/desert-outpost.md \
            js/data/desert/layout.js \
            js/data/desert/solids.js \
            js/data/desert/backgrounds.js \
            js/data/desert/engineering.js \
            js/runtime/tables-desert.js \
            tools/check-desert.cjs \
            tools/apply-desert-fill-under-arms.cjs \
            .github/workflows/apply-desert-fill-under-arms.yml
          git commit -m "redesign: remove empty space under Arms Dealer"
          git push origin HEAD:redesign/desert-fill-under-arms
