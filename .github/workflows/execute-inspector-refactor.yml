name: Apply verified inspector patch

on:
  push:
    branches: [fix/refactor-inspector-contracts]
    paths: [.github/workflows/execute-inspector-refactor.yml]

permissions:
  contents: write

jobs:
  apply:
    if: github.repository == 'k911mipt/terraria-base'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2
      - name: Apply tested patch to the exact audited base
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD^)" = 1c85ce806b7374add61bffeed7a26fd0dea962d0
          cat > /tmp/inspector.patch <<'PATCH'
          diff --git a/js/data/underground/materials.js b/js/data/underground/materials.js
          index 34c47e9..0f05846 100644
          --- a/js/data/underground/materials.js
          +++ b/js/data/underground/materials.js
          @@ -66,6 +66,7 @@ Object.assign(WALL_SPECS, {
               itemEn: "Gray Brick Wall",
               paintRu: "Зелёная краска",
               paintEn: "Green Paint",
          +    safe: true,
               note: "Главная безопасная стена мастерской Гоблина и короткого спуска к водоёму.",
             },
             copper_wall_plain: {
          @@ -73,6 +74,7 @@ Object.assign(WALL_SPECS, {
               itemEn: "Copper Brick Wall",
               paintRu: "Без краски",
               paintEn: "None",
          +    safe: true,
               note: "Тёплая рабочая панель за инструментами и зоной перековки.",
             },
           });
          @@ -96,6 +98,7 @@ Object.assign(WALL_SPECS, {
               itemEn: "Smooth Marble Wall",
               paintRu: "Розовая краска",
               paintEn: "Pink Paint",
          +    safe: true,
               note: "Основная декоративная панель комнаты Принцессы и фон её настенной полки.",
             },
           });
          diff --git a/js/runtime/core.js b/js/runtime/core.js
          index 5c2878f..af548e2 100644
          --- a/js/runtime/core.js
          +++ b/js/runtime/core.js
          @@ -80,12 +80,25 @@ function schedule() {
               });
           }
           
          -function roomAt(wx, wy) {
          +function roomAt(scene, wx, wy) {
             return (
          -    D.rooms
          +    scene.rooms
                 .filter((r) => wx >= r.x1 && wx <= r.x2 && wy >= r.y1 && wy <= r.y2)
                 .sort(
                   (a, b) => (a.x2 - a.x1) * (a.y2 - a.y1) - (b.x2 - b.x1) * (b.y2 - b.y1),
                 )[0] || null
             );
           }
          +
          +// Ownership and tile location are different at shared doors and room borders.
          +function roomForObject(scene, object) {
          +  if (!object) return null;
          +  // Legacy base doors use room: "" to mean no declared ownership.
          +  if (Object.hasOwn(object, "room") && object.room !== "") {
          +    const room = scene.rooms.find((candidate) => candidate.id === object.room);
          +    if (!room)
          +      throw new Error(`${scene.title}: object ${object.id} references unknown room ${object.room}`);
          +    return room;
          +  }
          +  return roomAt(scene, object.x, object.y);
          +}
          diff --git a/js/runtime/inspector.js b/js/runtime/inspector.js
          index 7207a66..58170bd 100644
          --- a/js/runtime/inspector.js
          +++ b/js/runtime/inspector.js
          @@ -3,6 +3,11 @@ function showTip(e, o) {
             if (drag?.moved) return;
             tip.style.display = "block";
             const parts = [`<b>${escHtml(o.name)}</b>`];
          +  const room = roomForObject(D, o);
          +  if (room)
          +    parts.push(
          +      `<div class="tip-section"><strong>Модуль объекта:</strong> ${escHtml(room.name)}</div>`,
          +    );
             if (o.engineering && o.kind !== "wire" && o.kind !== "futureTrap") {
               const fs = engineeringForegroundSpec(o);
               if (fs)
          diff --git a/js/runtime/model.js b/js/runtime/model.js
          index bb42e88..938a79b 100644
          --- a/js/runtime/model.js
          +++ b/js/runtime/model.js
          @@ -353,13 +353,22 @@ function objectsAtTile(x, y) {
             return out;
           }
           
          +// The absence of a wall is separate from missing or malformed metadata.
          +function wallSafetyLabel(spec, hasWall = true) {
          +  if (!hasWall) return null;
          +  if (spec?.safe === true) return "Да, поставленная игроком";
          +  if (spec?.safe === false) return "Нет";
          +  return "Ошибка данных: не указано safe";
          +}
          +
           function inspect(o, wx, wy) {
             selected = o || null;
             searchHit = null;
             const tx = Math.floor(wx),
               ty = Math.floor(wy);
             selectedTile = o ? null : { x: tx, y: ty };
          -  const room = roomAt(tx, ty),
          +  const room = roomAt(D, tx, ty),
          +    objectRoom = roomForObject(D, o),
               engDevice = engineeringDeviceAtTile(tx, ty),
               solid = rectAt(D.solids, tx, ty),
               bg = rectAt(D.backgrounds, tx, ty),
          @@ -398,6 +407,7 @@ function inspect(o, wx, wy) {
                 ],
                 ["Размер", `${o.w}×${o.h}`],
                 ["Тип объекта", o.kind],
          +      ["Модуль объекта", objectRoom?.name || "—"],
               );
               if (o.engineering) {
                 rows.push(["Инженерный этап", o.stage || ENG.stage]);
          @@ -466,7 +476,7 @@ function inspect(o, wx, wy) {
             }
             rows.push(
               ["Тайл", `x${tx}, y${ty}`],
          -    ["Модуль", room?.name || "—"],
          +    ["Область тайла", room?.name || "—"],
               ["Передний тип", bs.layer],
               ["Передний материал", biName(bs)],
               ["Краска блока", paintName(bs)],
          @@ -488,11 +498,8 @@ function inspect(o, wx, wy) {
                 ["Фоновый элемент", bg.name],
                 ["Назначение фона", bg.desc || "—"],
               );
          -  if (ws.safe !== null)
          -    rows.push([
          -      "Безопасная стена",
          -      ws.safe ? "Да, поставленная игроком" : "Нет",
          -    ]);
          +  const safety = wallSafetyLabel(ws, Boolean(bg));
          +  if (safety !== null) rows.push(["Безопасная стена", safety]);
             rows.push([
               "Объекты в тайле",
               cellObjects.length ? cellObjects.map((x) => x.id).join(", ") : "нет",
          diff --git a/js/runtime/tables-desert.js b/js/runtime/tables-desert.js
          index cf2a90e..2a506d1 100644
          --- a/js/runtime/tables-desert.js
          +++ b/js/runtime/tables-desert.js
          @@ -58,7 +58,7 @@ function populate() {
             document.getElementById("arenaRows").innerHTML = specObjects
               .map(
                 (o) =>
          -        `<tr><td><span class="mat-code">${escHtml(o.short || o.id)}</span></td><td>x${o.x}…${o.x + o.w - 1}, y${o.y}…${o.y + o.h - 1}</td><td>${escHtml(roomAt(o.x, o.y)?.short || "—")}</td><td>${escHtml(o.name)}</td><td>${escHtml(o.desc || "—")}</td></tr>`,
          +        `<tr><td><span class="mat-code">${escHtml(o.short || o.id)}</span></td><td>x${o.x}…${o.x + o.w - 1}, y${o.y}…${o.y + o.h - 1}</td><td>${escHtml(roomForObject(D, o)?.short || "—")}</td><td>${escHtml(o.name)}</td><td>${escHtml(o.desc || "—")}</td></tr>`,
               )
               .join("");
           
          diff --git a/js/runtime/tables-jungle.js b/js/runtime/tables-jungle.js
          index 4b21007..d44734b 100644
          --- a/js/runtime/tables-jungle.js
          +++ b/js/runtime/tables-jungle.js
          @@ -17,11 +17,6 @@ function escHtml(s) {
             );
           }
           
          -function moduleForObject(object) {
          -  const declaredRoom = D.rooms.find((room) => room.id === object.room);
          -  return declaredRoom || roomAt(object.x, object.y);
          -}
          -
           function populate() {
             const v = D.validation;
             document.getElementById("status").innerHTML = [
          @@ -63,7 +58,7 @@ function populate() {
               .filter((object) => !hiddenKinds.has(object.kind))
               .map(
                 (object) =>
          -        `<tr><td><span class="mat-code">${escHtml(object.short || object.id)}</span></td><td>x${object.x}…${object.x + object.w - 1}, y${object.y}…${object.y + object.h - 1}</td><td>${escHtml(moduleForObject(object)?.short || "—")}</td><td>${escHtml(object.name)}</td><td>${escHtml(object.desc || "—")}</td></tr>`,
          +        `<tr><td><span class="mat-code">${escHtml(object.short || object.id)}</span></td><td>x${object.x}…${object.x + object.w - 1}, y${object.y}…${object.y + object.h - 1}</td><td>${escHtml(roomForObject(D, object)?.short || "—")}</td><td>${escHtml(object.name)}</td><td>${escHtml(object.desc || "—")}</td></tr>`,
               )
               .join("");
           
          diff --git a/js/runtime/tables-underground.js b/js/runtime/tables-underground.js
          index 855805c..252fb9e 100644
          --- a/js/runtime/tables-underground.js
          +++ b/js/runtime/tables-underground.js
          @@ -17,11 +17,6 @@ function escHtml(s) {
             );
           }
           
          -function moduleForObject(object) {
          -  const declaredRoom = D.rooms.find((room) => room.id === object.room);
          -  return declaredRoom || roomAt(object.x, object.y);
          -}
          -
           function populate() {
             const v = D.validation;
             document.getElementById("status").innerHTML = [
          @@ -67,7 +62,7 @@ function populate() {
             document.getElementById("arenaRows").innerHTML = specObjects
               .map(
                 (object) =>
          -        `<tr><td><span class="mat-code">${escHtml(object.short || object.id)}</span></td><td>x${object.x}…${object.x + object.w - 1}, y${object.y}…${object.y + object.h - 1}</td><td>${escHtml(moduleForObject(object)?.short || "—")}</td><td>${escHtml(object.name)}</td><td>${escHtml(object.desc || "—")}</td></tr>`,
          +        `<tr><td><span class="mat-code">${escHtml(object.short || object.id)}</span></td><td>x${object.x}…${object.x + object.w - 1}, y${object.y}…${object.y + object.h - 1}</td><td>${escHtml(roomForObject(D, object)?.short || "—")}</td><td>${escHtml(object.name)}</td><td>${escHtml(object.desc || "—")}</td></tr>`,
               )
               .join("");
           
          diff --git a/js/runtime/tables.js b/js/runtime/tables.js
          index 2e288e8..1388c6c 100644
          --- a/js/runtime/tables.js
          +++ b/js/runtime/tables.js
          @@ -30,7 +30,7 @@ function populate() {
               )
               .join("");
             const storage = D.objects
          -    .filter((o) => o.kind === "chest" && o.room === "craft" && o.storageFloor)
          +    .filter((o) => o.kind === "chest" && roomForObject(D, o)?.id === "craft" && o.storageFloor)
               .sort((a, b) => a.id.localeCompare(b.id, undefined, { numeric: true }));
             document.getElementById("storageRows").innerHTML = storage
               .map(
          diff --git a/js/runtime/validation.js b/js/runtime/validation.js
          index aac5a70..6420ddb 100644
          --- a/js/runtime/validation.js
          +++ b/js/runtime/validation.js
          @@ -54,3 +54,26 @@ function validatePitConfiguration() {
               ENG.validation.errors.push("Таймеры должны стоять x1/y55 и x134/y55");
             ENG.validation.status = ENG.validation.errors.length ? "FAIL" : "PASS";
           }
          +
          +// Only materials actually referenced by the scene need a complete wall spec.
          +// A missing boolean is a data error, never evidence that a wall is unsafe.
          +function validateUsedWallSpecs(scene, specs) {
          +  const errors = [], checked = new Set();
          +  for (const region of scene.backgrounds) {
          +    if (checked.has(region.mat)) continue;
          +    checked.add(region.mat);
          +    const spec = specs[region.mat];
          +    const where = `${scene.title}: wall ${region.mat} at X${region.x1} Y${region.y1}`;
          +    if (!spec) {
          +      errors.push(`${where}: missing wall specification`);
          +      continue;
          +    }
          +    for (const field of ["itemRu", "itemEn", "paintRu", "paintEn", "note"]) {
          +      if (typeof spec[field] !== "string" || !spec[field].trim())
          +        errors.push(`${where}: missing or invalid ${field}`);
          +    }
          +    if (typeof spec.safe !== "boolean")
          +      errors.push(`${where}: missing or invalid safe (expected boolean)`);
          +  }
          +  return errors;
          +}
          diff --git a/tools/check-jungle.cjs b/tools/check-jungle.cjs
          index a17d7c6..7eea2d2 100644
          --- a/tools/check-jungle.cjs
          +++ b/tools/check-jungle.cjs
          @@ -28,16 +28,16 @@ const { D, ENG } = vm.runInContext(
           );
           
           vm.runInContext(
          -  fs.readFileSync(path.join(root, "js/runtime/tables-jungle.js"), "utf8"),
          +  fs.readFileSync(path.join(root, "js/runtime/core.js"), "utf8"),
             context,
          -  { filename: "js/runtime/tables-jungle.js" },
          +  { filename: "js/runtime/core.js" },
           );
           
           const displayedDoorModules = vm.runInContext(
             `Object.fromEntries(
               D.objects
                 .filter((object) => object.kind === "door")
          -      .map((object) => [object.id, moduleForObject(object)?.id || null])
          +      .map((object) => [object.id, roomForObject(D, object)?.id || null])
             )`,
             context,
           );
          diff --git a/tools/check-rooms.cjs b/tools/check-rooms.cjs
          new file mode 100644
          index 0000000..1ba7989
          --- /dev/null
          +++ b/tools/check-rooms.cjs
          @@ -0,0 +1,48 @@
          +#!/usr/bin/env node
          +"use strict";
          +
          +const assert = require("node:assert/strict");
          +const { loadScene, SCENES } = require("./lib/load-scene.cjs");
          +
          +for (const entry of SCENES) {
          +  const { run, runFile } = loadScene(entry);
          +  runFile("js/runtime/core.js");
          +  const snapshot = run("JSON.stringify(D)");
          +  // Validate all explicit references, including engineering objects shown in tips.
          +  run("[...D.objects, ...ENG.devices, ...ENG.futureSlots].forEach(o => roomForObject(D, o))");
          +  const doors = Array.from(run('D.objects.filter(o => o.kind === "door").map(o => o.id)'));
          +  for (const id of doors) {
          +    const key = JSON.stringify(id);
          +    const actual = run(`roomForObject(D, D.objects.find(o => o.id === ${key}))?.id || null`);
          +    const expected = run(`(() => {
          +      const o = D.objects.find(o => o.id === ${key});
          +      return o.room || roomAt(D, o.x, o.y)?.id || null;
          +    })()`);
          +    assert.equal(actual, expected, `${entry}: ${id}`);
          +  }
          +  assert.equal(run("roomAt(D, D.bounds.xMax + 100, D.bounds.yMax + 100)"), null);
          +  assert.equal(run("roomForObject(D, null)"), null);
          +  assert.throws(() => run('roomForObject(D, { id: "BAD_ROOM", x: 0, y: 0, room: "MISSING_ROOM" })'),
          +    /BAD_ROOM.*unknown room MISSING_ROOM/);
          +
          +  // Preserve the previous smallest-area rule, then original order for ties.
          +  run(`globalThis.overlapScene = { title: "fixture", rooms: [
          +    { id: "large", x1: 0, y1: 0, x2: 20, y2: 20 },
          +    { id: "first", x1: 2, y1: 2, x2: 10, y2: 10 },
          +    { id: "second", x1: 2, y1: 2, x2: 10, y2: 10 },
          +  ] }`);
          +  assert.equal(run('roomAt(overlapScene, 3, 3).id'), "first");
          +  assert.equal(run('roomForObject(overlapScene, { id: "NO_ROOM", x: 3, y: 3 }).id'), "first");
          +  assert.equal(run('roomForObject(overlapScene, { id: "LEGACY_EMPTY", x: 3, y: 3, room: "" }).id'), "first");
          +  assert.equal(run('roomForObject(overlapScene, { id: "EXPLICIT", x: 3, y: 3, room: "large" }).id'), "large");
          +  assert.equal(run('roomAt(overlapScene, 10, 10).id'), "first");
          +
          +  if (entry === "underground.html") {
          +    assert.equal(run('roomForObject(D, D.objects.find(o => o.id === "UG_MECH_GOBLIN")).id'),
          +      "underground_goblin");
          +    assert.equal(run('(() => { const o = D.objects.find(o => o.id === "UG_MECH_GOBLIN"); return roomAt(D, o.x, o.y).id; })()'),
          +      "underground_mechanic", "the geographical tile area intentionally differs from ownership");
          +  }
          +  assert.equal(run("JSON.stringify(D)"), snapshot, "lookups must not mutate geometry or order");
          +  console.log(`PASS ${entry}: shared object ownership, ${doors.length} boundary doors and negative cases`);
          +}
          diff --git a/tools/check-underground.cjs b/tools/check-underground.cjs
          index 430a6c7..2b8f806 100644
          --- a/tools/check-underground.cjs
          +++ b/tools/check-underground.cjs
          @@ -28,16 +28,16 @@ const { D, ENG } = vm.runInContext(
           );
           
           vm.runInContext(
          -  fs.readFileSync(path.join(root, "js/runtime/tables-underground.js"), "utf8"),
          +  fs.readFileSync(path.join(root, "js/runtime/core.js"), "utf8"),
             context,
          -  { filename: "js/runtime/tables-underground.js" },
          +  { filename: "js/runtime/core.js" },
           );
           
           const displayedDoorModules = vm.runInContext(
             `Object.fromEntries(
               D.objects
                 .filter((object) => object.kind === "door")
          -      .map((object) => [object.id, moduleForObject(object)?.id || null])
          +      .map((object) => [object.id, roomForObject(D, object)?.id || null])
             )`,
             context,
           );
          diff --git a/tools/check-wall-specs.cjs b/tools/check-wall-specs.cjs
          new file mode 100644
          index 0000000..2399e64
          --- /dev/null
          +++ b/tools/check-wall-specs.cjs
          @@ -0,0 +1,46 @@
          +#!/usr/bin/env node
          +"use strict";
          +
          +const assert = require("node:assert/strict");
          +const { loadScene, SCENES } = require("./lib/load-scene.cjs");
          +
          +for (const entry of SCENES) {
          +  const { run, runFile } = loadScene(entry);
          +  runFile("js/runtime/validation.js");
          +  runFile("js/runtime/model.js");
          +  const errors = () => Array.from(run("validateUsedWallSpecs(D, WALL_SPECS)"));
          +  assert.deepEqual(errors(), [], entry);
          +  assert.equal(run("wallSafetyLabel({ safe: true })"), "Да, поставленная игроком");
          +  assert.equal(run("wallSafetyLabel({ safe: false })"), "Нет");
          +  assert.equal(run("wallSafetyLabel({ safe: null }, false)"), null);
          +  assert.match(run("wallSafetyLabel({})"), /Ошибка данных.*safe/);
          +  assert.match(run("wallSafetyLabel({ safe: null })"), /Ошибка данных.*safe/);
          +
          +  // Negative checks exercise every USED wall, including per-scene extensions.
          +  const materials = Array.from(run("[...new Set(D.backgrounds.map(r => r.mat))]"));
          +  for (const material of materials) {
          +    const key = JSON.stringify(material);
          +    run(`globalThis.originalSpec = WALL_SPECS[${key}]; WALL_SPECS[${key}] = { ...originalSpec };`);
          +    run(`delete WALL_SPECS[${key}].safe`);
          +    assert(errors().some(message => message.includes(material) && message.includes("safe")),
          +      `${entry}: deleting safe from ${material} must be rejected`);
          +    run(`WALL_SPECS[${key}].safe = false`);
          +    assert.deepEqual(errors(), [], `${entry}: legitimate safe:false must stay valid`);
          +    for (const field of ["itemRu", "itemEn", "paintRu", "paintEn", "note"]) {
          +      run(`WALL_SPECS[${key}] = { ...originalSpec }; delete WALL_SPECS[${key}][${JSON.stringify(field)}]`);
          +      assert(errors().some(message => message.includes(material) && message.includes(field)),
          +        `${entry}: missing ${field} in ${material} must be rejected`);
          +    }
          +    run(`delete WALL_SPECS[${key}]`);
          +    assert(errors().some(message => message.includes(material) && message.includes("specification")));
          +    run(`WALL_SPECS[${key}] = originalSpec`);
          +  }
          +
          +  if (entry === "underground.html") {
          +    for (const [x, y] of [[24, 8], [29, 10], [44, 9]]) {
          +      assert.equal(run(`wallSafetyLabel(WALL_SPECS[rectAt(D.backgrounds, ${x}, ${y}).mat])`),
          +        "Да, поставленная игроком", `control tile X${x} Y${y}`);
          +    }
          +  }
          +  console.log(`PASS ${entry}: ${materials.length} used wall specs and negative mutations`);
          +}
          diff --git a/tools/lib/load-scene.cjs b/tools/lib/load-scene.cjs
          new file mode 100644
          index 0000000..2c53085
          --- /dev/null
          +++ b/tools/lib/load-scene.cjs
          @@ -0,0 +1,25 @@
          +"use strict";
          +
          +const fs = require("node:fs");
          +const path = require("node:path");
          +const vm = require("node:vm");
          +
          +const ROOT = path.resolve(__dirname, "../..");
          +const SCENES = ["index.html", "desert.html", "underground.html", "jungle.html"];
          +
          +// Read the actual entry point so checks use the same ordered data as the browser.
          +function loadScene(entry, root = ROOT) {
          +  const context = vm.createContext({ console });
          +  const run = (code) => vm.runInContext(code, context);
          +  const runFile = (file) => vm.runInContext(
          +    fs.readFileSync(path.join(root, file), "utf8"), context, { filename: file },
          +  );
          +  const html = fs.readFileSync(path.join(root, entry), "utf8");
          +  for (const match of html.matchAll(/<script\b[^>]*\bsrc="([^"]+)"/g)) {
          +    const file = match[1].replace(/^\.\//, "").split("?")[0];
          +    if (file.startsWith("js/data/")) runFile(file);
          +  }
          +  return { context, run, runFile };
          +}
          +
          +module.exports = { loadScene, SCENES, ROOT };
          PATCH
          git apply --check /tmp/inspector.patch
          git apply /tmp/inspector.patch
          for check in tools/check-*.cjs; do node "$check"; done
          git add js tools
          git diff --cached --check
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'fix: validate wall safety and unify object room ownership'
          git push origin "HEAD:${GITHUB_REF_NAME}"
