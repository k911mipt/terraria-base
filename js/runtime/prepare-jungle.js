// Empty engineering indexes keep the shared inspector unchanged while the
// world-specific teleporter route remains a future reserve.
prepareEngineering();
ENG.validation.heartTargets = "не используется";
ENG.validation.pitStates = "не используется";
