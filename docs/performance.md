# Профиль производительности (#37)

Измерение текущего нативного планировщика; никаких изменений production-кода,
стратегии Canvas-кешей, hit-testing, геометрии или эталона. Результаты времени
не являются CI-порогами: шумный runner не должен блокировать merge из-за скорости.
Ошибки запуска, ресурсов, выполнения жестов и неполный прогон являются ошибками.

```sh
# Из корня checkout, после установки requirements-dev.txt и Chromium:
python tools/profile-planner.py --repeats 5
python tools/check-performance.py
# После merge — повтор на GitHub Actions с текущим main:
gh workflow run profile-planner.yml --repo k911mipt/terraria-base --ref main
# Скачать конкретный завершённый запуск:
gh run download RUN_ID --repo k911mipt/terraria-base \
  --name planner-performance --dir performance-artifacts
```

## Методика

20 отдельных browser contexts: Main и Desert, desktop 1800×1200 и mobile
390×844, DPR 1, по пять повторов, чередующих сцены/viewport. Chromium headless
из закреплённого Playwright, без CPU/network throttling. Browser process и
файловый кеш ОС не перезапускаются: это новый context с холодным браузерным кешем,
**не холодный запуск ОС**. Mobile — эмуляция размеров/касания, не реальный Pixel.

Открываются настоящие HTML, CSS, import maps и ES-модули через loopback HTTP.
Ни маршрутизации ответов, ни замены исходников, ни отключения проверки версии.
Из события `planner-started` тест получает публичный API экземпляра. До загрузки
устанавливается наблюдатель первого `data-ready`: startupReadyMs отсчитывается от
начала навигации и включает загрузку/подготовку/первое рисование.

Перед вводом устанавливается visual и фокусируется крафтовая Main либо хаб
Desert через публичный API, с pad=12. Прямого изменения scale без пересчёта
центра нет. Требуются минимум три видимых предмета; их ID и камера сохраняются.
Desktop выполняет 80 настоящих mousemove по этим предметам и проверяет видимый
tooltip последней цели; обе конфигурации — pan и zoom
(mouse/wheel либо CDP touch/pinch). Камера обязана сдвинуться/увеличиться,
захваченные указатели — освободиться. Main дополнительно выполняет девять
переключений visual/arena/wiring при развёрнутой панели, после фокуса на
ENG.focus с pad=4. Камера этого workload также сохраняется; для pan/zoom
мобильная панель свёрнута. Результаты содержат полное время операции
с накладными расходами драйвера/ожидания кадров, CDP TaskDuration/ScriptDuration
и elapsed-время **исполнения callback rAF**, измеренное тестовой обёрткой. Это не время
GPU/compositor и не input-to-photon latency. Два служебных rAF для ожидания не
включаются в callback-выборку; отсутствие вызовов отмечается null, не нулём.

`objectAt` и `roomAt` измеряются отдельно по десять партий из 10 000 запросов,
после одной прогревочной партии каждой функции. 256 фиксированных координат
чередуют центры реальных объектов и точки по всей области. Возвращаемые ID
участвуют в checksum. Это стоимость изолированной функции в visual, не целого
hover-handler и не замер поиска инженерного overlay. Для результатов времени
публикуются исходные партии, median и nearest-rank p95. Для lookups это
распределение средних по партиям, а не хвост задержки отдельных запросов.
Для startup при n=5 p95 равен максимуму.

## Три разные величины памяти

1. **Расчёт RGBA:** реальные width×height×4 каждого cache/screen Canvas. Это
   арифметический бюджет RGBA8, не измерение выделенной памяти Chrome/GPU.
2. **V8 heap:** Runtime.getHeapUsage после ready, после ввода до GC и после
   принудительного GC. Изменение heap не равно сумме аллокаций и не доказывает
   утечку. Отдельные `.heapprofile` получены sampling-профилировщиком с интервалом
   4096 байт, включая уже собранные объекты. Профилируется отдельный прогон
   lookups (по 20 000 запросов на функцию, включая прогрев), не смешанный с timing.
   Сумма sample.size — статистическая оценка выборки, не точный счётчик аллокаций;
   стек может включать измерительный код/Playwright.
3. **RSS процессов Chromium:** CDP сообщает PID, Linux `/proc/PID/status` —
   snapshot VmRSS по процессам. Сумма повторно учитывает shared pages, относится
   ко всему браузеру, не выделяет Canvas и не является пиком или PSS/USS. При
   недоступности метода/процесса записывается причина, не выдуманный ноль.

Фактическое отдельное потребление Canvas/GPU и поведение физического телефона
этим инструментом не измеряются. Из этих данных нельзя объявлять утечку памяти.

## Сохранение и интерпретация

`performance-artifacts/summary.json`: ревизия checkout, SHA-256 всех файлов
приложения, окружение, полные профили и сводки. Для каждого повтора отдельные
JSON, PNG, sampled allocation profile. Даже при сбое summary сохраняет FAIL и
уже завершённые повторы; отдельный JSON содержит исходную ошибку. Успех не
подтверждается размером снимка. Измерения не коммитятся автоматически.

Workflow `profile-planner.yml` read-only, запускается вручную или при PR,
меняющем сам profiler; он не заменяет обязательный `validate`. Семь тестов
измерителя входят в общую `python tools/validate.py`, но timing-порогов нет.

### Источники измерительных API

- [Runtime.getHeapUsage](https://chromedevtools.github.io/devtools-protocol/tot/Runtime/#method-getHeapUsage)
- [HeapProfiler.startSampling](https://chromedevtools.github.io/devtools-protocol/tot/HeapProfiler/#method-startSampling)
- [Performance.getMetrics](https://chromedevtools.github.io/devtools-protocol/tot/Performance/#method-getMetrics)

## Результат настоящего HTTP-прогона — 10 сентября 2026

[Run 34502240769](https://github.com/k911mipt/terraria-base/actions/runs/34502240769),
artifact `10162380204`, PR head `6f97d4a9`; checkout merge `84bea335`.
Полные 20/20 профилей прошли без JS/сетевых ошибок. Приложение совпадает с
main `da89383a`: SHA-256 `ed746f261f1370d94cf49c32210e1525aad63514ffd4e9a995f8cee26c9a3fe3`.
[Компактные измеренные результаты](performance-baseline.json) сохранены отдельно
от геометрического эталона и не используются как пороги тестов.

Окружение: Ubuntu runner, Linux 6.17.0-1022-azure, строка модели CPU AMD EPYC
7763 64-Core Processor (это имя CPU, не заявление о 64 выделенных vCPU),
Python 3.12.14, Playwright 1.57.0, Chromium 143.0.7499.4. Реальный Pixel/Safari
не измерялся. Снимки обеих сцен на desktop/mobile просмотрены.

| Сцена / viewport | Ready median / max, мс | objectAt median / p95, мкс | roomAt median / p95, мкс | pan/zoom rAF median / p95, мс |
| --- | --- | --- | --- | --- |
| Main desktop | 329,3 / 367,1 | 6,810 / 7,04 | 0,100 / 0,31 | 3,4 / 3,7 |
| Main mobile | 317,7 / 331,2 | 6,745 / 7,40 | 0,100 / 0,32 | 1,0 / 4,6 |
| Desert desktop | 183,1 / 186,6 | 0,835 / 1,26 | 0,120 / 0,27 | 0,5 / 0,6 |
| Desert mobile | 167,5 / 169,5 | 0,850 / 1,73 | 0,145 / 0,29 | 0,5 / 1,8 |

Main: 368×72 тайла, 348 объектов, 18 областей; Desert: 80×71, 38 объектов,
6 областей. Для выбора целей доступны 223/228 предметов Main и 31/32 Desert
(desktop/mobile), но каждый desktop workload выполняет ровно 80 наведений.
Во всех 10 desktop-сессиях hover не запрашивал rAF: Canvas не перерисовывался
от одного наведения, а DOM-tooltip последнего предмета проверен явно.
Медианная ScriptDuration фазы 80 mousemove: 14,355 мс Main и 13,414 мс Desert;
TaskDuration — 295,810 и 227,069 мс соответственно. Это суммарная работа renderer
в фазе с драйвером/tooltip, не задержка отдельного ввода. Девять переключений
инженерных режимов Main: rAF median/p95 3,8/4,4 мс desktop и 1,1/1,7 мс mobile.

| Сцена / viewport | Cache RGBA8, MiB (расчёт) | Screen RGBA8 при ready, MiB (расчёт) | V8 used при ready / после GC, MiB (median) | Сумма RSS при ready, MiB (median) |
| --- | --- | --- | --- | --- |
| Main desktop | 77,625 | 14,904 | 7,431 / 4,208 | 607,71 |
| Main mobile | 77,625 | 2,470 | 7,480 / 4,210 | 485,77 |
| Desert desktop | 16,641 | 14,904 | 4,205 / 2,723 | 498,73 |
| Desert mobile | 16,641 | 2,470 | 4,177 / 2,772 | 401,14 |

Main имеет три кэша 5888×1152, Desert — 1280×1136. Screen измерен **при ready**,
до сворачивания мобильной панели; последующий resize меняет экранные буферы.
Нельзя вычитать V8/RGBA из RSS и объявлять остаток памятью Canvas/GPU.
Общий browser process между contexts сохраняется; RSS может включать его кеши.

Отдельные sampling-прогоны lookups оценили аллокации в 69,95/70,12 MiB Main и
12,01/12,00 MiB Desert (desktop/mobile, medians). Это 40 000 запросов вместе с
прогревом на прогон, не объём постоянно удерживаемой памяти. В профиле стеков
видно выделение временных массивов objectAt; оценки разных представлений
sampling-profile нельзя механически складывать как точные физические байты.

**Решение: по измеренному workload runtime-оптимизация не требуется.** Повторная
фильтрация выделяет временные массивы, однако lookup занимает единицы микросекунд
и наведение не вызывает перерисовку всей карты. Не получено обоснования менять
семантику поиска, стратегию кешей или вводить индексы/WebGL/workers. Замеры не
доказывают отсутствие всех возможных задержек: GPU/input-to-photon не измерены,
настоящий телефон не профилирован. Высокий RGBA-бюджет основной карты остаётся
известным ограничением, не доказательством утечки или OOM. При жалобе на конкретное
устройство нужен его профиль, а не экстраполяция headless-runner.

## Отброшенные предварительные прогоны

Run `34500186062` завершился FAIL: измеритель выбирал режим при свёрнутой шторке.
В run `34500784822` все профили завершились, но самопроверка выявила
нерепрезентативный фокус hover/pan: прямое присваивание scale после fit могло
показывать пустое небо. Оба запуска исключены из численного отчёта выше.
В итоговом `34502240769` камера задаётся через API, предметы видимы, tooltip
проверяется, инженерные переключения выполняются над реальной зоной устройств.
Ни один из этих исправленных дефектов измерителя не менял приложение.


## Изоляция повторных запусков

Профилировщик атомарно создаёт новый каталог `--output`. Если путь уже
существует (даже пустой каталог, файл или symlink), команда завершается
с кодом 2 **до** запуска браузера и записи отчёта. Старые JSON, PNG и
heapprofile не удаляются и не перезаписываются. Для повторения используйте
новый путь, например `python tools/profile-planner.py --repeats 1 --output
performance-artifacts/run-2` (команда одной строкой). Это исключает смешение
пяти старых повторов с одним новым или старого PASS с неудачным запуском.

В CI измерения лежат в `performance-artifacts/profiles/`; файлы provenance
от gh остаются уровнем выше. Исторические артефакты, на которые ссылается
отчёт, сохраняют прежнюю структуру. Шесть дополнительных регрессий проверяют
новые/уже занятые каталоги, сохранность результатов, файлы, symlink и ранний
отказ CLI. Геометрия, методика измерений и baseline-числа не меняются.
